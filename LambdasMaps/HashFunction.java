package HashMap;

/* 
 * Enum representing different types of hash functions.
 * Not used in this version but was used in the last one
 */
public enum HashFunction {
   NAIVE,          // Represents a naive hash function
   SOPHISTICATED   // Represents a sophisticated hash function using compression
}
